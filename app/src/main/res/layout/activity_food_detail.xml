<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF8F9"
    android:padding="24dp">

    <!-- back button -->
    <ImageButton
        android:id="@+id/back_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_back"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Back"
        android:layout_alignParentStart="true"
        android:layout_marginTop="36dp" />

    <!-- food name -->
    <TextView
        android:id="@+id/foodNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/back_button"
        android:layout_marginTop="32dp"
        android:fontFamily="sans-serif-medium"
        android:text="Food Name"
        android:textColor="#EDAFB8"
        android:textSize="26sp"
        android:textStyle="bold" />

    <!-- Info card container -->
    <LinearLayout
        android:id="@+id/nutritionCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/foodNameTextView"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="32dp"
        android:background="@drawable/bg_yellow"
        android:elevation="6dp"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- calories -->
        <TextView
            android:id="@+id/caloriesTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:fontFamily="sans-serif-medium"
            android:text="Calories: 200 kcal"
            android:textColor="#444444"
            android:textSize="18sp" />

        <!-- proteins -->
        <TextView
            android:id="@+id/proteinsTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:fontFamily="sans-serif-medium"
            android:text="Proteins: 10 g"
            android:textColor="#444444"
            android:textSize="18sp" />

        <!-- carbs -->
        <TextView
            android:id="@+id/carbsTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:fontFamily="sans-serif-medium"
            android:text="Carbohydrates: 20 g"
            android:textColor="#444444"
            android:textSize="18sp" />

        <!-- fats -->
        <TextView
            android:id="@+id/fatsTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="Fats: 15 g"
            android:textColor="#444444"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- Save button -->
    <Button
        android:id="@+id/saveFoodButton"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="24dp"
        android:backgroundTint="#EDAFB8"
        android:elevation="8dp"
        android:padding="12dp"
        android:text="Save to My Meals"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

</RelativeLayout>
